[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    ".git",
    ".venv",
    "venv",
    "data",
    "logs",
    "audio_files",
]

[tool.ruff.lint]
select = ["E9", "F", "I"]

[tool.ruff.format]
quote-style = "preserve"
indent-style = "space"
line-ending = "lf"
